<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Макет страниц магазина
    |--------------------------------------------------------------------------
    |
    | Blade-макет, который расширяют страницы корзины и оформления. В нём
    | должны быть секции `title` и `content`.
    |
    */

    'layout' => env('NEXOR_SHOP_LAYOUT', 'site.layout'),

    /*
    |--------------------------------------------------------------------------
    | Адреса страниц
    |--------------------------------------------------------------------------
    */

    'routes' => [
        'cart' => env('NEXOR_SHOP_CART_PATH', 'cart'),
        'checkout' => env('NEXOR_SHOP_CHECKOUT_PATH', 'checkout'),
    ],

    /*
    |--------------------------------------------------------------------------
    | Корзина
    |--------------------------------------------------------------------------
    |
    | Корзина гостя живёт в зашифрованной cookie. Когда появится личный
    | кабинет, она будет объединяться с корзиной аккаунта.
    |
    */

    'cookie' => [
        'name' => 'nexor_cart',
        'minutes' => 60 * 24 * 30,
    ],

    // Больше строк cookie не выдержит, а человеку столько и не нужно.
    'max_lines' => 50,

];
